spring:
  application:
    name: ollama-ai
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: deepseek-r1:1.5b
          temperature: 0.7

server:
  port: 8081

logging:
  level:
    org:
      springframework: 
        ai:
          chat:
            client:
              advisor: DEBUG